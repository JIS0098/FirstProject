import styled from "styled-components";

const StyledDiv = styled.div`
  font-size: 20px;
`;

function StyleReference() {
  return <StyledDiv>ㅎㅇ</StyledDiv>;
}

export default StyleReference;
